package com.laidback.repository;

import com.laidback.model.RepositoryProcess;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryProcessRepository extends JpaRepository<RepositoryProcess, Integer> {
}
