package com.laidback.repository;

import com.laidback.model.RepositoryMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryMediaRepository extends JpaRepository<RepositoryMedia, Integer> {
}
